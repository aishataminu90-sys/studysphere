info:
  name: "Get all reminders"
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}/reminders"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
