info:
  name: "Register user "
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/auth/register"
  body:
    type: json
    data: |-
      {
        "name": "Ai",
        "email": "ai@test.com",
        "password": "678901",
        "university": "Griffith",
        "year": "2",
        "course": "Computing"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
