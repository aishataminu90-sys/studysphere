info:
  name: "Complete reminder"
  type: http
  seq: 14

http:
  method: PATCH
  url: "{{baseUrl}}/reminders/PASTE_REMINDER_ID_HERE/complete"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
