info:
  name: "Create reminder"
  type: http
  seq: 11

http:
  method: POST
  url: "{{baseUrl}}/reminders"
  body:
    type: json
    data: |-
      {
        "title": "Study Chapter 4",
        "description": "Focus on database indexing",
        "dueDate": "2026-04-18T10:00:00.000Z"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
