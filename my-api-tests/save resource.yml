info:
  name: "save resource "
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/resources"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
