info:
  name: "Get all groups"
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/groups"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
